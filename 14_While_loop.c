// ONE
/*In the C programming language, semicolons are not used within the while loop itself, but they are used to terminate statements within the loop's body.*/

#include <stdio.h>
int main()
{
    int i = 0;
    while (i<54)
    {
        printf("%d ", i);
        i = i + 1;
    }
     
    
    return 0;
}
